# Runtime dependencies, installed into a venv by install.sh. See NOTES.md ("Install").

# Volumio's socket.io server needs the socketio 4.x / engineio 3.x line.
python-engineio==3.14.2
python-socketio[client]>=4,<5

# GPIO. RPi.GPIO and spidev deliberately come from apt instead, not from here.
pigpio

# REST API server
fastapi
uvicorn

# Retry helper used by the Volumio client
tenacity

# LCD menu library (forked). Pinned to the dimming branch; --force-reinstall to update.
git+https://github.com/domb84/rpi-lcd-menu.git@add-dimming-capability
